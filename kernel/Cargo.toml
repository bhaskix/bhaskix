[package]
name = "bhaskix-kernel"
description = "The Bhaskix nucleus."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true

[lib]
name = "bhaskix_kernel"
path = "src/lib.rs"

[dependencies]
bhaskix-boot = { path = "../boot/handoff" }
bhaskix-arch-x86-64 = { path = "../arch/x86_64" }
bhaskix-mm = { path = "../mm" }

[lints]
workspace = true

[package.metadata.bhaskix]
# Confined to `sync` (the spinlock's UnsafeCell), `framebuffer` (the pixel
# blitter), `trap` (control-register reads), and `faultinject` (deliberately
# faulting instructions). No other module may contain `unsafe`; see
# docs/coding-style.md §3.
#
# Measured: 24 at M1, 57 after exceptions, 119 after interrupt bring-up.
# The last increase is the APIC and PIC setup sequence in `trap::enable` and
# the end-of-interrupt calls in the dispatcher, none of which can be expressed
# safely -- they are device register writes with ordering requirements.
# 197 used at M3. The increase is `memory.rs` turning a physical address into
# the `&'static mut [Frame]` backing the frame database, and `vm.rs` driving
# the page-table walker and zeroing freshly allocated frames.
unsafe_budget = 220
